name: Fork CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fork-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  core-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
      - uses: actions/setup-python@v6
        with:
          python-version-file: .python-version
      - name: Install locked development dependencies
        working-directory: hindsight-api-slim
        run: uv sync --frozen --group dev --extra all
      - name: Run maintained-patch regressions
        working-directory: hindsight-api-slim
        run: >-
          uv run --frozen --extra all pytest
          tests/test_ocr_quality.py
          tests/test_consolidation_failure_isolation.py
          tests/test_consolidation_prompt_budget.py
          tests/test_db_abstraction.py
          tests/test_language_integrity.py
          tests/test_language_integrity_retain.py
          tests/test_consolidation_retry_budget.py
          tests/test_fact_extraction_retry.py
          tests/test_content_policy_refusal_permanent.py
          tests/test_operation_status.py
          tests/test_response_schema_validation.py
          tests/test_metrics.py
          tests/test_batch_api.py
      - name: Run generated-client discriminator regressions
        run: |
          uv run --directory hindsight-clients/python --frozen pytest tests/test_operation_details.py
          (cd hindsight-clients/go && go test -run '^TestOperationResponseDetailsUseDiscriminator$' .)
      - name: Lint and format check
        working-directory: hindsight-api-slim
        run: |
          uv run --frozen ruff check .
          uv run --frozen ruff format --check .
      - name: Type check
        working-directory: hindsight-api-slim
        run: uv run --frozen ty check hindsight_api
      - name: Build and import smoke test
        working-directory: hindsight-api-slim
        run: |
          uv build
          uv run --frozen python -m compileall -q hindsight_api
          uv run --frozen python -c "import hindsight_api, hindsight_api.config, hindsight_api.main"
